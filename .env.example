# =====================================================
# Toolbox — environment variables
# =====================================================
# 注意：Vision LLM 的 provider + 模型 + API Key 已经改成
# 通过【设置页 → AI 模型】配置，保存在 ~/.toolbox/llm.json，
# 不再需要在这里填 key。这个文件仅保留给未来扩展用。
#
# 当前没有必须设置的环境变量，可以不创建 .env。
