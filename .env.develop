# Toolbox 开发环境变量
# 复制为 .env 后本地启动使用

# 管理员令牌（开发环境随便填，留空则写操作返回 503）
TOOLBOX_ADMIN_TOKEN=dev-token

# 限流关闭，方便本地测试
TOOLBOX_RATE_LIMIT=

# 上传大小上限（MB）
TOOLBOX_MAX_UPLOAD_MB=100

# 开启 Swagger UI（http://127.0.0.1:8000/docs）
TOOLBOX_DEBUG=1
